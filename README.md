# Vision_Transformer-Cats&Dogs
Vision Transformer (ViT) built from scratch — patch embeddings, CLS token, positional encoding, 4 transformer blocks — classifying images as cat or dog. Small (~8MB) model bundled with an interactive Streamlit app for instant predictions.
